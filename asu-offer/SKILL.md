---
name: asu-offer
description: 中文秋招求职进度管理技能：记录和更新投递、筛选、测评、面试、Offer、拒信及招聘邮件状态，生成可搜索的进度表；当用户要求管理秋招进度、记录投递状态、做求职漏斗表时使用。
---

# asu-offer：秋招进度管理

把零散的招聘邮件、截图和聊天记录整理成可复盘的求职漏斗，记录公司、岗位、日期、状态、下一步和备注。不得把申请编号、密码、验证码或无关个人信息写入公开资源。

## 资源定位

本技能自带后备可编辑进度表 `assets/application-tracker.html`（可编辑的求职进度表）。优先使用该后备模板；若用户提供了官方 `application-tracker.html` 与预览资源，可替换。使用后备模板时，在交付说明中写明"当前使用后备模板"。

## 工作流程

1. 从用户提供的邮件、招聘网站或截图中提取日期、公司、岗位、当前状态、下一步和必要备注。
2. 合并同一公司同一岗位的重复记录；后来的测评、面试、拒信或 Offer 状态覆盖早期的"已投递"。
3. 只在有证据时更新状态；普通自动回执不能推断为面试或 Offer，证据不足标记为"待确认"。
4. 用户未指定文件位置时，将 `application-tracker.html` 复制到桌面（或用户指定目录），并返回生成文件的绝对路径。
5. 用户要求查看邮箱或每日检查时，使用浏览器处理；登录、密码、MFA、验证码和 CAPTCHA 由用户完成，不代为保存。
6. 用户需要同时制作简历文件时，转入 asu-resume；需要改写经历时，转入 asu-experience。

## 状态规范

默认状态包括：已投递、筛选中、测评中、面试、Offer、拒绝/已结束、待确认。每次更新尽量写明证据来源和下一步动作，便于后续复盘。

## 默认交付

默认提供进度表文件、已识别记录摘要、状态变化、下一步清单和缺失信息。不要把用户的真实求职记录写进技能的模板或 README。
